# HTML Semantic Element

A simple HTML page showcasing semantic elements, with a companion stylesheet.

## Project Structure

- `index.html` - main HTML page
- `css/style.css` - styles for the page

## Usage

Open `index.html` in a browser to view the page.

